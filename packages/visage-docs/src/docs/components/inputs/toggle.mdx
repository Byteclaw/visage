---
title: 'Toggle component'
components: ['Toggle']
---

# Toggle

## Import

```js transpile=false
import { Toggle } from '@byteclaw/visage';
```

## Examples

### Default

```jsx live=true
<Toggle label="Toggle label" />
```

### Invalid

```jsx live=true
<Toggle invalid label="Toggle label" />
```

### Disabled

```jsx live=true
<Fragment>
  <Toggle disabled label="Disabled toggle"></Toggle>
  <Toggle disabled checked label="Disabled checked toggle"></Toggle>
</Fragment>
```

### Read only

```jsx live=true
<Fragment>
  <Toggle readOnly label="Read only toggle"></Toggle>
  <Toggle readOnly checked label="Read only toggle"></Toggle>
</Fragment>
```

### Hidden label

```jsx live=true
<Fragment>
  <Toggle hiddenLabel label="Toggle label" />
  <Toggle checked hiddenLabel label="Toggle label" />
</Fragment>
```

### With state content

```jsx live=true
<Toggle leftContent="off" rightContent="on" label="Toggle label" />
```

### Controlled

```jsx live=true
() => {
  const [checked, setChecked] = useState(false);

  return (
    <Toggle
      checked={checked}
      label="Controlled label"
      onChange={e => setChecked(e.currentTarget.checked)}
    />
  );
};
```

## Keyboard shortcuts

<DescriptionList>
  <DescriptionListItem
    term={<KeyboardKey>Tab</KeyboardKey>}
    description={
      <>
        If a Toggle isn&apos;t disabled and read only:
        <ul>
          <li>
            When the Toggle does not have focus, places focus on the Toggle.
          </li>
          <li>
            When the Toggle has focus, moves focus to the next element in the
            tab sequence.
          </li>
        </ul>
      </>
    }
  />
  <DescriptionListItem
    term={<KeyboardKey>Space</KeyboardKey>}
    description={
      <ul>
        <li>When a Toggler has focus, toggles it&apos;s state</li>
      </ul>
    }
  />
</DescriptionList>

## Faces

- `ToggleContainer`
- `ToggleControl`
- `ToggleLabel`
- `ToggleLabelText`
- `ToggleToggler`

## Prop Types

<PropTypes component="Toggle" />
